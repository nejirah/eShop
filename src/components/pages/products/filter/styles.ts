import styled from '@emotion/styled';
import { Accordion, Grid, Slider } from '@mui/material';

export const GridStyled = styled(Grid)`
  width: 100%;
  padding: 15px;
`;

export const SliderStyled = styled(Slider)`
  width: 70%;
  margin-left: 15px;
`;

export const AccordionStyled = styled(Accordion)`
  border: 0;
  box-shadow: none;
`;
